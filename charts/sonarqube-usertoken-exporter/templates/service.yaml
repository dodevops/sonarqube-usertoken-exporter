apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarqube-usertoken-exporter.fullname" . }}
  labels:
    {{- include "sonarqube-usertoken-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonarqube-usertoken-exporter.selectorLabels" . | nindent 4 }}
